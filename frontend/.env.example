# 后端 API 地址（端口由 port-manager 分配，后端服务见 docs/PORT_ALLOCATION_GUIDE.md）
VITE_API_BASE=http://127.0.0.1:8002

# 前端开发服务器端口（Vite dev server，由 port-manager 分配；vite.config.ts 读 process.env.VITE_PORT，根目录 ./start.sh 会把这里的值导出给 Vite）
VITE_PORT=8003
