# 后端 API 地址（一般不用设置）
# 留空时前端走同源相对路径 /api/...，由 Vite dev server 反向代理到后端
# （见 vite.config.ts 的 server.proxy，目标 = http://127.0.0.1:$VITE_API_PORT）。
# 这样别人通过局域网地址访问前端时，浏览器请求的也是同一台机器的前端端口，
# 因此远程访问无需任何额外配置。
# 只有当前后端不在同一台机器、或不走代理时，才需要显式指定绝对地址，例如：
# VITE_API_BASE=http://127.0.0.1:8002

# 后端端口（Vite 代理目标，默认 8002；根目录 ./start.sh 会按实际后端端口导出）
VITE_API_PORT=8002

# 前端监听地址：0.0.0.0 = 允许局域网/远程访问；127.0.0.1 = 只允许本机
VITE_HOST=0.0.0.0

# 前端开发服务器端口（Vite dev server，由 port-manager 分配；vite.config.ts 读 process.env.VITE_PORT，根目录 ./start.sh 会把这里的值导出给 Vite）
VITE_PORT=8003
