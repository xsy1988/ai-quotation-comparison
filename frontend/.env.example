# 后端 API 地址（端口由 port-manager 分配，后端服务见 docs/PORT_ALLOCATION_GUIDE.md）
VITE_API_BASE=http://127.0.0.1:8002

# 前端开发服务器端口（Vite dev server，由 port-manager 分配，写死在 vite.config.ts 的 server.port）
# 前端 dev server: 127.0.0.1:8003
